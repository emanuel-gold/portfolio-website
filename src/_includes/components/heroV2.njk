{#V2 Hero with no Skill Chips#}
{% import "components/icons.njk" as icons %}
<section id="top" class="bg-slate-100 bg-gradient-to-b from-light-gradient-start to-white dark:from-gray-950 dark:to-dark-gradient-end">
  <div class="mx-auto w-full max-w-6xl px-5 sm:px-8">
    <div class="lg:flex grid-cols-2 items-center gap-10 py-14 sm:py-16">
      <div class="content-wrapper flex flex-col basis-[56%] min-[1030px]:basis-7/12 text-center items-center max-w-screen-sm mx-auto lg:mx-0 lg:items-start lg:text-left">
        <p class="mb-3 inline-flex items-center gap-2 text-xs uppercase opacity-70">
          <span class="h-2 w-2 rounded-full bg-emerald-500"></span> {{ site.hero.availability }}
        </p>
        <h1 class="mb-4 font-black">{{ site.hero.heading | safe }}</h1>
        <p class="mb-8 max-w-prose text-lg opacity-90 ">{{ site.hero.summary }}</p>
        <div class="flex flex-col sm:flex-row justify-center w-full lg:justify-start flex-wrap gap-6">
          <a
            href="{{ site.hero.primaryCta.href }}"
            class="cta-solid"
          >
            {{ icons.mail('w-5 h-5') }} {{ site.hero.primaryCta.label }}
          </a>
          <a
            href="{{ site.hero.secondaryCta.href }}"
            class="cta-transparent"
            data-scroll
          >
            {{ icons.file_text('w-5 h-5') }} {{ site.hero.secondaryCta.label }}
          </a>
        </div>
        <div class="mt-6 flex items-center gap-4 text-sm opacity-80">
          <span class="inline-flex items-center gap-1">{{ icons.map_pin('w-4 h-4') }} {{ site.hero.location }}</span>
          {#
          <a href="{{ site.hero.github.href }}" class="inline-flex items-center gap-1 hover:opacity-100">
            {{ icons.github('w-4 h-4') }} {{ site.hero.github.label }}
          </a>
          #}
        </div>
      </div>
      <div class="chip-wrapper relative hidden basis-auto min-[1030px]:basis-5/12 lg:block">
        <div class="pointer-events-none absolute -inset-4 -z-10 rounded-3xl bg-gradient-to-tr from-fuchsia-500/10 via-cyan-400/10 to-emerald-400/10 blur-2xl"></div>
        <div class="grid grid-cols-2 gap-4 max-w-screen-sm md:max-w-xl mx-auto sm:gap-6" data-hero-chips>
          {% for chip in site.hero.highlightChips %}
            <div class="flex flex-col justify-center align-center rounded-2xl border p-4 text-center text-xs lg:text-sm font-medium shadow-sm dark:border-brand-dark-shadow/45"><span >{{ chip }}</span></div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</section>
