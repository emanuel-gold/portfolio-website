<section id="about" class="scroll-mt-24 py-14 sm:py-16">
  <div class="mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8">
    <div class="mb-10">
      {% if site.about.eyebrow %}
      <div class="mb-2 inline-block rounded-full border px-3 py-1 text-xs uppercase tracking-wider opacity-70">{{ site.about.eyebrow }}</div>
      {% endif %}
      <h2 class="text-3xl font-bold tracking-tight sm:text-4xl">{{ site.about.title }}</h2>
    </div>
    <div class="grid grid-cols-1 items-start gap-8 md:grid-cols-3">
      <div class="md:col-span-2">
        {% for paragraph in site.about.paragraphs %}
          <p class="mb-4 leading-relaxed opacity-90">{{ paragraph }}</p>
        {% endfor %}
        <ul class="list-inside list-disc opacity-90">
          {% for point in site.about.bulletPoints %}
            <li>{{ point }}</li>
          {% endfor %}
        </ul>
      </div>
      <div class="rounded-2xl border p-5 bg-white dark:bg-gray-950 dark:border-gray-800">
        <h4 class="mb-3 font-semibold">Quick facts</h4>
        <dl class="grid grid-cols-1 gap-3 text-sm">
          {% for fact in site.about.facts %}
            <div class="flex items-center justify-between">
              <dt class="opacity-70">{{ fact.term }}</dt>
              <dd>{{ fact.description }}</dd>
            </div>
          {% endfor %}
        </dl>
      </div>
    </div>
  </div>
</section>
